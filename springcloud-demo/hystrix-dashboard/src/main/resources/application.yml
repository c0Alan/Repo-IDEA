server:
  port: 1301
spring:
  application:
    name: hystrix-dashboard

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1001/eureka/

loggin:
  file: logs/${spring.application.name}.log